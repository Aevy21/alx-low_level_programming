#include "main.h"

/**
* _puts -a function that prints a string followed by a new line to stdout
*@str:string to be printed
*/

void _puts(char *str);
{
while (*str != '\0')
{
_puts(*str);
str++;
}
_puts('\n')
}
