#include <string.h>
#include "main.h"

/**
* print_rev - Prints a string in reverse f
ollowed by a new line.
* @s: The string to be printed in reverse.
*/
void print_rev(char *s) {
int length = strlen(s);
i=0;
for (int i = length - 1; i >= 0; i--) {
_putchar(s[i]);
}
_putchar('\n');
}

