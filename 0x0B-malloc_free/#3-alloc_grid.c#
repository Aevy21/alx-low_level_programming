#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 *alloc_grid -a function that returns a pointer to a 2 dimensional array of integers.
 * @grid: the address of the two dimensional grid
 * @width: width of the grid
 * @heipght: height of the grid
 *
 * Return: Nothing.
 */
int **al
loc_grid(int width, int height)
{
  if (width <= 0 || height <= 0) {
        return NULL;
    }
int **grid = (int **)malloc(height * sizeof(int *));
    if (grid == NULL) {
        return NULL;
    }
    for (int i = 0; i < height; i++)
      {
	grid[i] = (int *)malloc(width * sizeof(int));
	if (grid[i] == NULL) {
	  for (int j = 0; j < i; j++) {
                free(grid[j]);
		{
                free(grid[j]);
	
  int w;
    int h;

    h = 0;
    while (h < height)
    {
        w = 0;
        while (w < width)
	  {w++;
	  }
	h++
	  }
    if 